package aldb;

import static org.junit.Assert.*;
import static aldb.Solver.isSolvable;

import org.hamcrest.*;
import org.junit.*;

/**
 * Tests Solver.isSolvable
 */
public class IsSolvableTests {
  @Test
  public final void testShouldReturnTrueForModelWithoutConstraints() {
    //PrimSig A = new PrimSig("A");
    //assertTrue(isSolvable(Arrays.asList(new Sig[]{A})));
  }
}
